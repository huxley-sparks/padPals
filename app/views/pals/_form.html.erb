<%= form_for(@pal) do |f| %>
  <% if @pal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pal.errors.count, "error") %> prohibited this pal from being saved:</h2>

      <ul>
      <% @pal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
